name: mdbx
version: 0.1.0

authors:
  - mentalblood0 <necheporenko.stepan@yandex.ru>

crystal: '>= 1.17.0'

scripts:
  postinstall: cd src/ && git clone --depth 1 https://github.com/erthink/libmdbx/ && cd libmdbx && git fetch --tags --force --prune && cmake . && make lib

license: MIT
