name: mdbx
version: 0.1.0

authors:
  - necheporenko_s_iu <necheporenko_s_iu@goznak.ru>

crystal: '>= 1.17.0'

scripts:
  postinstall: cd src/ && git clone --depth 1 https://github.com/erthink/libmdbx/ && cd libmdbx && cmake && make lib

license: MIT
